This is an Anki addon for editing images while making or editing notes.

Uses Method-Draw for image editing.

To download git submodule Method-draw,
```shell
git submodule update --init --recursive
```